id: '006'
title: alert-engine
type: feature
risk: medium
depends_on:
- '003'
- '004'
context_files:
- src/alerts/alert_engine.py
done_when:
- Create src/alerts/alert_engine.py
- Define a function exactly named 'evaluate_alerts' that takes uptime_results (list)
  and ssl_results (list)
- 'The function must return a list of alert dictionaries, each with keys: site_name,
  alert_type, severity, message, triggered_at'
- 'alert_type must be one of: ''site_down'', ''ssl_expiring'', ''ssl_expired'', ''slow_response'''
- 'severity must be one of: ''critical'', ''warning'', ''info'''
- Generate a 'site_down' alert with severity 'critical' when is_up is False
- Generate a 'ssl_expired' alert with severity 'critical' when days_until_expiry <=
  0
- Generate a 'ssl_expiring' alert with severity 'warning' when days_until_expiry <=
  14
- Generate a 'slow_response' alert with severity 'warning' when response_time_ms >
  3000
- Define a function exactly named 'format_alert_text' that takes an alert dictionary
  and returns a human-readable string
- Define a function exactly named 'send_webhook_alert' that takes a webhook_url string
  and an alert dictionary, and posts the alert as JSON to the webhook URL using requests
- Add a if __name__ == '__main__' block that demonstrates alert evaluation with sample
  data
validate:
- python3 -m py_compile src/alerts/alert_engine.py
- grep 'def evaluate_alerts' src/alerts/alert_engine.py
- grep 'def format_alert_text' src/alerts/alert_engine.py
- grep 'def send_webhook_alert' src/alerts/alert_engine.py
- grep 'site_down' src/alerts/alert_engine.py
- grep 'ssl_expiring' src/alerts/alert_engine.py
- grep 'ssl_expired' src/alerts/alert_engine.py
- grep 'slow_response' src/alerts/alert_engine.py
status: done
metrics:
  tokens_sent: '4400'
  tokens_received: '710'
  cost: 0.00028
completed_at: '2026-03-05 00:28:41'
